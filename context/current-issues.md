### Fixed in code
- ✅ **Link Department names from registered managers to employees** — Converted the `Department` input to a `Select` dropdown in Step 2. Automatically extracts unique departments from registered managers and filters the `Reports to` dropdown to only show managers from the selected department.

- ✅ **Admin onboarding profile update sends email/hangs** — ...